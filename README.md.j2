{% extends "base.md.j2" %}

{% block screenshots %}
{{ screenshot("graph", alt="The graph editor wiring sensors, curves and fans on one canvas") }}

<details>
<summary>More screenshots</summary>

{{ screenshot("fans", alt="Dashboard fan cards showing where each control sits on its curve") }}

{{ screenshot("curve-editor", alt="A point curve with hysteresis and response limiting in the node panel") }}

{{ screenshot("group", alt="A group opened as a sub-canvas with its input and output connectors") }}

</details>
{% endblock %}

{% block install %}
## Installation

Build and install the daemon and CLI as a systemd service:

```sh
sudo packaging/install.sh
sudo systemctl enable --now galed.service
```

This installs `galed` and `gale` to `/usr/local/bin`, the systemd unit, and the
udev rules that grant hidraw access to supported Corsair devices. Uninstall
with `sudo packaging/install.sh --uninstall`.

### Windows

Download `gale-windows-x86_64.zip` from the
[latest release](https://github.com/Garulf/gale/releases/latest), extract it,
then from an elevated PowerShell in that folder:

```powershell
.\install.ps1
```

This registers `galed` as a Windows service and starts it, and registers a
tray icon that starts with your session and shows the service status.
Uninstall with:

```powershell
.\install.ps1 -Uninstall
```

Motherboard fan control on Windows goes through the Nuvoton Super I/O chip and
needs the [PawnIO](https://pawnio.eu) driver installed; the same driver backs the
AMD CPU, DIMM and ASUS embedded-controller sensors. Corsair and NVIDIA control
work without it.
{% endblock %}

{% block usage %}
## Usage

### Features

- Curve-driven fan control over hwmon (motherboard/CPU headers on Linux) and four
  Corsair device families: Commander Core, Commander Pro, Corsair PSU, and Hydro Platinum
- NVIDIA GPU fan control via NVML, including true fan stop: a duty of 0 hands the fans
  back to the driver's own policy, because NVML clamps a manual level to the card's minimum
- A REST and WebSocket API served alongside an embedded web UI, no separate
  frontend to install
- Windows sensors through the same PawnIO driver FanControl uses: the Nuvoton Super I/O
  chip, AMD Ryzen CPU temperatures (Tctl, Tdie, per-CCD) and package power, DDR4 and DDR5
  DIMM temperatures, and ASUS embedded-controller sensors such as the chipset fan
- Readings beyond temperature and rpm, usable as curve inputs and shown on the dashboard:
  CPU usage, clock and package power; GPU usage, VRAM, core and memory clocks, power draw
  and performance state
- Curve types: point, linear, trigger, target, flat, and the duty operators max, min,
  average, sum, subtract, offset, and sync
- Virtual sensors: max, min, mean (optionally smoothed over a window), sum, subtract, offset and
  delta nodes that combine or transform sensors before a curve reads them, plus
  webhook nodes that take their value from an HTTP POST
- Graph editor: one canvas per profile wiring device sensors, virtual sensors, curves
  and device controls together, with live values on every port and edge, and groups that
  fold a set of nodes into one node you can open as a sub-canvas
- Per-control limits with a calibration routine that finds a fan's stop, start and
  minimum duty by sweeping it against its own tachometer
- Home Assistant integration over MQTT discovery
- Journaled claims so a killed daemon restores prior fan state on the next start

### Quick start

`galed` reads its config from the path in `GALE_CONFIG` (default
`/etc/gale/config.toml`). A minimal config assigning a point curve to a
motherboard PWM header:

```toml
tick_interval_ms = 200
active_profile = "default"

[api]
bind = "127.0.0.1:5250"

[profiles.default.curves.cpu]
type = "point"
sensor = "hwmon/nct6798/temp1"
points = [[30.0, 20.0], [70.0, 100.0]]

[profiles.default.assignments]
"hwmon/nct6798/pwm1" = "cpu"
```

Start the daemon (or `systemctl start galed` once installed) and open
`http://127.0.0.1:5250` for the dashboard and graph editor, or drive it with
the `gale` CLI.

### Graph editor

The Graph page is the primary way to build a profile. Every hardware sensor and
control shows up automatically as a node with one handle per channel; dragging an
edge from a sensor into a curve or virtual sensor wires it, and dragging an edge
into a control handle is the assignment, replacing the old assignment table.
Curves, virtual sensors and mix or sync nodes sit between the two, so a max of two
temperature probes feeding a point curve that drives three fans is a handful of
drags rather than three separate forms. Node positions are saved with the profile
under `[ui.graph.<profile>]` so a layout survives a reinstall. Saving applies the
whole profile through the same validation the API always enforced, and any warning
comes back attached to the node it concerns.
The node menu is organised by operation: curves, temperature operators (max, min,
average, sum, subtract, offset, delta, webhook) and duty operators (max, min, average, sum, subtract, offset, sync),
so a max-of-two-probes node and a max-of-two-curves node are separate entries rather
than one node with a mode switch.

Each hardware control can carry its own limits under `[controls."<id>"]`: a stop
duty below which the fan snaps to 0, a start duty that kicks it from a stop, a hard
minimum and a maximum ceiling. They apply to whatever curve drives the control and
are edited from the control node's panel, where Detect limits sweeps the fan against
its paired tachometer and fills them in from where it actually stalls and restarts.

A curve's x axis follows the kind of sensor feeding it, so a curve on a GPU memory
clock is drawn in MHz and one on package power in watts, and the axis grows to fit
readings past its default. Rpm and duty sensors are not offered as curve inputs.

Selecting several nodes and pressing Ctrl+G folds them into a group node. Double
clicking it opens the group as its own canvas with the rest of the profile hidden,
and connector strips at the left and right edges carry the group's inputs and
outputs, so wiring across the boundary works from either side. Groups are a view
over the same flat profile: they are stored under `[ui.groups.<profile>]` and the
daemon never reads them.

Curve nodes can load a shape from a preset. Quiet, Balanced and Performance ship
built in, and Save as stores the node's current shape under `[presets.<name>]` in
config.toml so it follows the config file and can be loaded onto any other curve.
Hardware channels can be renamed from the device node panel; the names are stored
under `[labels]` keyed by sensor or control id and apply to the dashboard, the graph
and the CLI inventory alike. Clearing a label restores the hardware name.

### Home Assistant

Set `[mqtt] enabled = true` with your broker's `host`, `port`, `username` and `password`
and Gale announces itself through MQTT discovery: every hardware and virtual sensor
becomes a Home Assistant sensor, every fan control becomes a duty number plus a manual
switch, and a select entity switches the active profile. Values publish on change and
in full once a minute, commands arrive on the `gale/control/<id>/set_duty`,
`gale/control/<id>/set_manual` and `gale/profile/set` topics, and the daemon's last
will marks everything unavailable if it drops off the broker. `discovery_prefix` and
`topic_prefix` are configurable. The password is redacted from `GET /api/config` and
kept when a config is saved without it, like the API key.

### Virtual sensors

A profile can define derived sensors under `[profiles.<name>.sensors]`. They are
addressed as `virtual/<id>` anywhere a sensor id is accepted, including other
virtual sensors, and show up in the dashboard and `GET /api/status` like any
hardware sensor.

```toml
[profiles.default.sensors.cpu_hot]
type = "max"
inputs = ["superio/nct6798d/temp2", "nvidia/0/temp"]

[profiles.default.sensors.coolant_smooth]
type = "mean"
inputs = ["corsair/commander-pro-0805009c9327/temp1"]
window_s = 10

[profiles.default.sensors.coolant_rise]
type = "delta"
input = "virtual/coolant_smooth"
window_s = 30

[profiles.default.sensors.gpu_adjusted]
type = "offset"
input = "nvidia/0/temp"
add = -5.0
scale = 1.0

[profiles.default.curves.case]
type = "point"
sensor = "virtual/cpu_hot"
points = [[35.0, 25.0], [75.0, 100.0]]
```

| type | inputs | output |
| ------ | ------ | ------ |
| `max`, `min` | one or more sensor ids | the extreme of the inputs that have a value; unavailable only when every input is |
| `mean` | one or more ids, optional `window_s` | the mean of the available inputs, smoothed over `window_s` seconds when set |
| `offset` | one id, `add`, `scale` | `input * scale + add` |
| `delta` | one id, `window_s` | change in degrees per minute over the window, positive when rising |
| `webhook` | none; `token`, optional `timeout_s` | the last value received by `POST /api/webhook/<token>`; unavailable until the first POST and, when `timeout_s` is set, after that many seconds without one |

A virtual sensor with no value behaves like an unplugged probe: the curve reading it
has no input and its fan falls back to 100 percent. Cycles between virtual sensors
and references to undefined ones are rejected when the config is loaded. Missing
hardware behind a virtual sensor is reported in `GET /api/warnings` by its hardware
id.

### Webhook sensors

A `webhook` sensor is a virtual sensor with no upstream inputs: a script, Home
Assistant, or any other service pushes a reading into it over HTTP and it behaves
like a hardware probe from then on.

```toml
[profiles.default.sensors.room]
type = "webhook"
token = "replace-me-with-the-output-of-openssl-rand-hex-32"
timeout_s = 120.0
```

```sh
curl -X POST http://127.0.0.1:5250/api/webhook/replace-me-with-the-output-of-openssl-rand-hex-32 \
  -H 'Content-Type: application/json' -d '{"value": 23.5}'
```

The body is `{"value": <number>}` or `{"value": <bool>}`; `true` maps to `1.0` and
`false` to `0.0`, so a boolean feeding a 0 to 100 duty range goes through an
`offset` node with `scale = 100.0`. The token in the URL is the only credential, the
same model Discord and GitHub use for incoming webhooks: the endpoint ignores
`[api] api_key`, an unknown token answers 404, and the daemon binds to
`127.0.0.1` by default so the URL is only reachable from the machine unless you
change `[api] bind`. Creating the node in the graph editor generates a 64
character token on the first save and shows the full URL with a copy button in the
node's panel (`GET /api/webhook-url/<name>` behind the API key). When writing the
config by hand, put any long random string in `token`, for example the output of
`openssl rand -hex 32`. Without `timeout_s` the last value never expires; with it,
the sensor reads as unavailable once that many seconds pass without a POST, and
any curve reading it falls back to 100 percent through the usual path. Values are
not kept across a daemon restart.

### API

The daemon exposes REST and WebSocket endpoints under `/api`, all guarded by
an optional API key set in `[api] api_key`, except the webhook endpoint, which
is guarded by its own token:

| Method | Path                      | Description                          |
| ------ | ------------------------- | ------------------------------------ |
| GET    | `/api/status`             | Daemon status                        |
| GET    | `/api/inventory`          | Detected sensors and controls, plus the active profile's virtual sensors |
| GET    | `/api/config`             | Current config                       |
| PUT    | `/api/config`             | Replace and reload the config        |
| GET    | `/api/config.toml`        | The config as TOML, secrets redacted |
| GET    | `/api/warnings`           | Non-fatal startup and runtime issues |
| PUT    | `/api/profiles/:name`     | Create or replace a profile          |
| DELETE | `/api/profiles/:name`     | Delete a profile, 422 on the active one |
| POST   | `/api/profiles/:name/activate` | Switch the active profile       |
| GET    | `/api/presets`            | Built-in and user curve presets      |
| PUT/DELETE | `/api/presets/:name`  | Save or remove a user preset         |
| PUT/DELETE | `/api/labels/*id`     | Rename a hardware channel, or restore its name |
| PUT/DELETE | `/api/control-settings/*id` | Set or clear a control's duty limits |
| POST/GET/DELETE | `/api/calibrate/*id` | Start, poll or cancel a fan calibration sweep |
| PUT    | `/api/controls/*id`       | Set a manual override duty           |
| DELETE | `/api/controls/*id`       | Clear a manual override              |
| POST   | `/api/webhook/:token`     | Push `{"value": <number or bool>}` into a webhook sensor; the token is the credential, no API key |
| GET    | `/api/webhook-url/:name`  | Full POST URL for a webhook sensor, built from the request's Host header |
| GET    | `/api/ws`                 | WebSocket stream of live sensor/control state |
{% endblock %}

{% block extra %}
## Hardware support

| Family | Status |
| ------ | ------ |
| hwmon (motherboard, CPU, Linux) | supported |
| Corsair Commander Pro | supported, verified on hardware |
| Corsair Commander Core | supported, not verified on hardware |
| Corsair PSU | supported, not verified on hardware |
| Corsair Hydro Platinum | supported, not verified on hardware |
| NVIDIA GPU (NVML) | supported, verified on hardware |
| Nuvoton NCT67xx Super I/O (Windows, PawnIO) | supported, verified on an NCT6798D |
| AMD Ryzen CPU temperatures and package power (Windows, PawnIO) | supported, verified on a Ryzen 7 5800XT |
| DDR4 and DDR5 DIMM temperatures (Windows, PawnIO) | supported, verified on DDR4 |
| ASUS embedded controller (Windows, PawnIO) | supported, verified on a PRIME X570-PRO |
| CPU usage and clock | supported on Linux and Windows |
| AMD GPU | not supported |

### Release semantics

Not every backend can return a control to automatic hardware behavior the
same way:

- Commander Core and Corsair PSU restore hardware auto mode when a control is
  released.
- Commander Pro and Hydro Platinum have no unset available upstream, so what
  happens on release there is configurable via `[hardware.corsair]
  on_release` in the config: `"pin_full"` writes 100%, `"keep_last"` leaves
  the last duty in place, and `{ fixed = <percent> }` writes a fixed duty.
  The default is `"keep_last"`.
- On Hydro Platinum, the first `set_duty` call also pins the sibling fans on
  the same device to 100% and the pump to its balanced curve, since the
  protocol addresses the whole device rather than one channel.
- Getting a Commander Pro or Hydro Platinum back onto an iCUE hardware
  profile after Gale has been driving it needs a power cycle or iCUE itself;
  none of the `on_release` modes hand control back to firmware curves.

### Sharing devices with other software

Super I/O access is coordinated through the ISA bus mutex, so `galed`
coexists with FanControl and HWiNFO polling the same motherboard chip.
Corsair USB devices cannot be shared that way: HID replies carry no request
id, so running FanControl's Corsair plugin, iCUE, or liquidctl against a
device Gale is also probing can make Gale read their traffic as its own.
Stop the other program's access to a Corsair device before pointing Gale at
it, or leave that device out of Gale's config.
{% endblock %}

{% block license %}
## License

GPL-3.0-or-later for the daemon, CLI and hardware drivers. The curve engine
(`gale-core`) is also available under MIT so it can be reused in permissive projects.
See NOTICE for the projects Gale's drivers are derived from.
{% endblock %}
