[package]
name = "gale-hw-cpu"
version.workspace = true
edition.workspace = true
license = "GPL-3.0-or-later"

[dependencies]
gale-hw = { path = "../gale-hw" }
tracing = "0.1"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = ["Win32_Foundation", "Win32_System_Performance", "Win32_System_Power", "Win32_System_Threading"] }

[dev-dependencies]
tempfile = "3"
